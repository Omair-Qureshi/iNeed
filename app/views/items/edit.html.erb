 <div class="header">
    <h1>Edit quote</h1>
  </div>

  <%= turbo_frame_tag "item_#{@item.id}" do %>
    <%= simple_form_for @item do |f| %>
      <%= f.input :name %>
      <%= f.input :description %>
      <%= f.button :submit %>
    <% end %>
  <% end %>
