Details of list
<%= @list.user.first_name %>
<ul>
<% @list.items.each do |item| %>
<li> <%= item.name %>: <%= item.description %> </li>
<li> <%= link_to "Mark as OK", item_path(item), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%> </li>
<% end %>
</ul>
<%= link_to "back to lists", lists_path %>
